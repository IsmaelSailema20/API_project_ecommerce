import { TiposIdentificacionEntity } from './tipos-identificacion.entity';

describe('TiposIdentifcadores', () => {
  it('should be defined', () => {
    expect(new TiposIdentificacionEntity()).toBeDefined();
  });
});
