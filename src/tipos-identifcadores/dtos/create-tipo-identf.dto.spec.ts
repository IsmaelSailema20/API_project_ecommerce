import { CreateTipoIdentfDto } from './create-tipo-identf.dto';

describe('CreateTipoIdentfDto', () => {
  it('should be defined', () => {
    expect(new CreateTipoIdentfDto()).toBeDefined();
  });
});
